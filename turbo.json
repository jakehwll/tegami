{
  "$schema": "https://turbo.build/schema.json",
  "globalEnv": ["PUBLIC_URL", "PORT", "NEXTAUTH_URL", "NEXTAUTH_SECRET"],
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", ".next/**"]
    },
    "lint": {
      "outputs": []
    },
    "dev": {
      "cache": false
    },
    "db:generate": {
      "cache": false
    },
    "db:push": {
      "cache": false
    }
  }
}
